<%= form_for(@intern_question) do |f| %>
  <% if @intern_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@intern_question.errors.count, "error") %> prohibited this intern_question from being saved:</h2>

      <ul>
      <% @intern_question.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :value %><br />
    <%= f.check_box :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
